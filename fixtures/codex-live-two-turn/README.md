# Fixture boundary

This directory is copied to a temporary workspace for live certification.
Only `TASK.md` and this README are copied. `expected/result.txt` remains outside
the Codex workspace and is used solely by the certification verifier.
