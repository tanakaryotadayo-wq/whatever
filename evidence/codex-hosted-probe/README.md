# Codex hosted provider probe

The GitHub-hosted macOS probe records whether an `OPENAI_API_KEY` repository secret is available and, when present, runs the official Codex App Server three-run certification. No secret value is written to evidence.
