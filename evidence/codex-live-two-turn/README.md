# Codex live evidence

The self-hosted provider gate writes three per-run evidence directories,
generated protocol artifacts, and `certification-receipt.json` here. Runtime
evidence is uploaded as a GitHub Actions artifact and is not committed as a
PASS until the official binary completes all three runs.
