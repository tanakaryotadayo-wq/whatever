# Codex App Server Live Certification Status

Status: **IMPLEMENTED / HOSTED CONTRACT PASS / SELF-HOSTED RUNNER QUEUED**  
Branch: `akashic/v0.10-codex-app-server-live`  
Head: `a15ed61093601aa60d64ec80c8286bd4c7c166da`

The official App Server adapter, fake protocol server, isolated two-turn fixture,
sanitized evidence writer, schema generation, timeout interruption, and
three-consecutive-run gate are implemented.

GitHub Actions run `31926377835` completed the hosted contract job successfully.
The official subscription-auth provider job exists and is queued for the
self-hosted runner labels `self-hosted`, `macOS`, and `akashic-codex`.

No live provider PASS is claimed until that job produces a certification
receipt and evidence artifact.
