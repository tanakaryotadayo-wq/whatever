# Akashic v0.7.0-rc1 — Core Closure

Status: **CORE_CLOSED / EXTERNAL_ACCEPTANCE_OPEN**

## Closed

- canonical GitHub workspace and authority boundaries;
- Temporal `RunAgentTask` fixture vertical slice;
- validated Context Delta CAS and durable input wait;
- one-turn Activity worker with idempotency/cancel/restart fail-closed;
- immutable ArtifactRef, Effect Ledger, verification and fenced adoption;
- Drive immutable/projection adapter contract;
- hardened Vercel MCP → Temporal control path;
- Cloudflare demoted to non-authoritative conformance experiment;
- clean-room workspace, CI, release evidence, and asset recovery ledger.

## External acceptance still required

- official authenticated Codex app-server same-thread live two-turn evidence;
- real Drive resumable upload/appProperties/projection evidence;
- authenticated ChatGPT → Vercel → Temporal mutation evidence.

The `rc1` label is deliberate. Core implementation is merged, but production completion must not be claimed until all three external evidence gates pass.
