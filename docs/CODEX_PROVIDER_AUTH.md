# Codex Provider Authentication Boundary

Official live certification may use an existing subscription-authenticated self-hosted runner or a GitHub-hosted runner with a repository-scoped `OPENAI_API_KEY` secret. Credentials must not be committed, echoed, uploaded, or included in sanitized protocol evidence.
